package com.ds.hashing;

public class Hashing {
	/***
	 * @author Utkarsh Rathore
	 * 
	 * To use a hash function that converts a given key to a smaller number and uses the small number as index in a table called hash table.
	 */
}
