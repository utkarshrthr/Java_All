package com.design.pattern.builder;

public class Builder {

	// Query
	// select statement
	// select statement where clause
	// select statement where clause order by
	// select statement where clause group by
	
	// QueryBuilder  -> build() -> Query
}
