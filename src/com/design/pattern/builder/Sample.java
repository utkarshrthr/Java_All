package com.design.pattern.builder;

public class Sample {

	 public static void main(String[] args) {
		 Query query = new QueryBuilder("select query").build();
		 //Query(query, null, null, null, having, )
		 // Money, MoneyBuilder, getMoney
	}
}

